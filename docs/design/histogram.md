# pg_exporter Snapshot Histogram Specification

| Field | Value |
| --- | --- |
| Status | Final |
| Version | 1.3 |
| Last updated | 2026-07-17 |
| Scope | SQL observations aggregated into classic Prometheus bucket series |
| Authority | Normative design for the first pg_exporter Histogram implementation |

## 1. Purpose

This document specifies the first Histogram capability for `pg_exporter`.
It is the source of truth for configuration, SQL result interpretation,
in-memory aggregation, Prometheus exposition, reference collectors, and test
acceptance criteria.

The design intentionally provides one user-facing `HISTOGRAM` usage. Version 1
does not expose separate `GAUGE_HISTOGRAM` and `COUNTER_HISTOGRAM` usages.

The key words **MUST**, **MUST NOT**, **SHOULD**, **SHOULD NOT**, and **MAY**
are to be interpreted as normative requirements.

## 2. Decision summary

Version 1 makes the following decisions:

1. `HISTOGRAM` means a **snapshot distribution rebuilt from SQL rows during
   one real query execution**.
2. Each non-NULL SQL value is one observation. Rows sharing the same complete
   label tuple belong to the same logical Histogram.
3. Histogram state MUST NOT survive a real query execution. A scrape cache hit
   reuses the cached snapshot and MUST NOT add observations.
4. The exporter MUST expose the logical Histogram as cumulative bucket, count,
   and sum **Gauge series**. It MUST NOT use `prometheus.NewConstHistogram` in
   version 1.
5. Bucket series may increase or decrease between snapshots. PromQL consumers
   MUST NOT apply `rate()`, `irate()`, `increase()`, or reset logic to them.
6. Version 1 supports raw observations only. SQL-provided pre-aggregated
   buckets, counter Histograms, formal OpenMetrics GaugeHistogram exposition,
   Prometheus Native Histograms, and OTLP Histograms are out of scope.
7. A query execution that contains Histogram columns is published atomically.
   No scalar or Histogram component from a failed execution may be emitted.

This is a deliberate semantic contract, not an assertion that counter and
gauge Histograms are generally interchangeable. All initial PostgreSQL use
cases describe the current population and therefore have gauge-like temporal
behavior.

## 3. Rationale

The current reference use cases are the per-database distributions of:

1. open transaction age, over every client backend that holds an `xact_start`;
2. idle-in-transaction age, over the idle-in-transaction subset of the same
   backends.

Every population can shrink, disappear, or move between buckets. For example,
an open transaction disappears when it commits or rolls back, while a
long-running transaction moves from smaller age buckets to larger age buckets.
These values are not monotonic across scrapes.

The Go `prometheus.Collector` interface does not require separate collector
implementations for counter and gauge Histograms. The row grouping and bucket
aggregation algorithm is identical. The distinction matters at the temporal
and exposition layers.

`client_golang` v1.23.2 has no high-level constant GaugeHistogram constructor.
`prometheus.NewConstHistogram` produces a counter-like Histogram family. It can
render a correct instant distribution, but it assigns misleading counter
semantics to a snapshot that may decrease. Version 1 therefore emits explicit
Gauge series and accepts that the metric metadata is not a formal
GaugeHistogram family.

## 4. Terminology

**Observation**
: One finite numeric value returned by SQL for a configured Histogram column.

**Label tuple**
: The ordered values of every `LABEL` column in the query, after existing label
  renaming and casting rules are applied.

**Logical Histogram**
: The distribution for one Histogram column and one label tuple during one
  real SQL execution.

**Finite bucket**
: A configured inclusive upper boundary. The `+Inf` bucket is generated by the
  exporter and is never configured.

**Snapshot**
: The complete logical Histogram result produced by one real SQL execution.
  A cached response is the same snapshot, not a new observation period.

## 5. Configuration contract

### 5.1 Column definition

The canonical version 1 syntax reuses the existing `Column.Bucket` field and
its singular YAML key:

```yaml
- seconds:
    usage: HISTOGRAM
    bucket: [1, 3, 10, 30, 100, 300, 1000, 3000, 10000, 30000, 100000]
    description: Open transaction age snapshot in seconds
```

`usage` remains case-insensitive during parsing and MUST be normalized to
`HISTOGRAM`.

The existing options have these meanings:

- `name` and `rename` determine the logical base metric name in the same way as
  Gauge and Counter columns.
- `scale` is applied to the SQL value before bucket assignment and summation.
  As with scalar columns, timestamp and boolean values are exempt from `scale`.
- `default`, when explicitly configured, converts SQL NULL to one observation
  with the configured default value. The default is multiplied by `scale`,
  matching the existing scalar metric behavior. Without `default`, NULL is
  ignored.
- `description` is the common help-text prefix for derived series.

### 5.2 Bucket validation

A Histogram column MUST configure at least one finite bucket.

Configured buckets MUST:

- be valid finite `float64` values;
- be strictly increasing;
- contain no duplicates;
- contain neither `NaN` nor positive or negative infinity;
- omit `+Inf`, which is generated automatically;
- use the final exported unit after `scale` has been applied to observations.

Invalid Histogram configuration MUST fail configuration parsing. It MUST NOT
be deferred until scrape time.

Only finite upper boundaries are configured. The first classic bucket has the
implicit mathematical interval `(-Inf, b0]`, where `b0` is the first configured
boundary. Users MUST NOT configure `-Inf`; for finite observations an
`le="-Inf"` bucket would always be empty and carries no information. Values
below `b0`, including negative values, are counted in the first bucket.

For a domain that legitimately contains signed observations, users SHOULD add
finite negative boundaries appropriate to the domain and a `0` boundary, for
example `[-100, -10, -1, 0, 1, 10, 100]`. This preserves useful resolution on
both sides of zero; it does not change the implicit lower edge. The reference
PostgreSQL observations are non-negative and are clamped in SQL.

### 5.3 Query column classification

`HISTOGRAM` columns are metric columns. A query is valid when it defines at
least one `GAUGE`, `COUNTER`, or `HISTOGRAM` column.

The existing query-level label model remains unchanged: every logical metric
in one query uses the same ordered label names. If two Histograms require
different labels, they MUST be placed in different query definitions.

### 5.4 Reserved and derived names

For a logical base name `<base>`, version 1 derives:

- `<base>_bucket{le="..."}`;
- `<base>_count`;
- `<base>_sum`.

Within one query definition that contains a Histogram:

- `le` MUST NOT be a query label after label rename;
- derived names MUST be valid Prometheus metric names;
- derived names MUST NOT collide with any scalar or derived metric family in
  that query after rename;
- two Histogram columns MUST NOT resolve to the same base name after rename;
- help text for all instances of each derived family MUST be identical.

Name and query-label conflicts MUST be rejected during per-query configuration
validation. Identical derived families MAY appear in mutually exclusive
version branches when type, help text, label names, and bucket boundaries are
identical.

A runtime constant label named `le` conflicts when any parsed query contains a
Histogram. This MUST be rejected during exporter creation and
configuration reload by the validation path that has both constant labels and
parsed queries. It cannot be validated by `ParseConfig` alone.

## 6. SQL result contract

### 6.1 Raw observations

Version 1 accepts only raw observations. For each result row:

1. label columns form the row's label tuple;
2. each Histogram column independently contributes zero or one observation;
3. a NULL Histogram value contributes no observation unless `default` is set;
4. a finite numeric value contributes exactly one observation after scaling.

Multiple Histogram columns MAY be returned by one SQL query. They are grouped
independently but share the query's label tuple.

### 6.2 Invalid values

An observation that cannot be converted to a finite `float64` is a collection
error. The collector MUST discard the entire new query result, including any
scalar Gauge or Counter metrics already built from earlier rows, and report the
query as failed. It MUST NOT expose a partially updated Histogram snapshot or a
mixture of new scalar metrics and failed Histogram metrics.

All scalar and Histogram component metrics for a query containing Histograms
MUST therefore be buffered outside `q.result` until row iteration, row-error
checking, value validation, and Histogram materialization all succeed. Only
then may the new result be assigned to `q.result`.

The version 1 implementation applies the same pending-result publication path
to scalar-only queries. This is a deliberate compatibility hardening: a
mid-iteration driver error no longer exposes a partial scalar prefix, and
metric-construction errors are reported instead of panicking. It does not
change successful scalar metric names, types, labels, or values.

Negative observations are valid at the generic aggregation layer. Reference
time-age collectors MUST clamp or filter impossible negative values in SQL.

### 6.3 Missing columns

A configured Histogram column missing from the SQL result is a collection
error. A missing Histogram column MUST NOT silently produce an empty
distribution.

### 6.4 Empty populations

A label tuple with zero valid observations for a Histogram is not emitted.
Version 1 does not synthesize empty label tuples that are absent from SQL.
Reference SQL MAY generate explicit population rows when stable zero-valued
series are required, but NULL still contributes no observation.

### 6.5 Unsupported inputs

Version 1 does not accept:

- SQL arrays of bucket counts;
- one row per bucket boundary;
- weighted observations;
- cumulative counts supplied by SQL;
- delta windows;
- quantiles supplied by SQL.

These inputs require a separate, future input-layout contract.

## 7. Aggregation algorithm

### 7.1 Group identity

The aggregation key is the pair:

```text
(Histogram column identity, complete ordered label tuple)
```

The label tuple MUST use a collision-safe representation. A plain delimiter
join is not sufficient because label values may contain the delimiter.

### 7.2 Accumulator

Each logical Histogram accumulator contains:

```text
labels       ordered label values
count        uint64 observation count
sum          float64 observation sum
bucketCount  one uint64 count for each configured finite upper boundary
```

`bucketCount[i]` is cumulative and counts all observations `<= bucket[i]`.
An implementation MAY store disjoint bucket populations internally, but it
MUST convert them to cumulative values before exposition.

### 7.3 Observation assignment

For every finite observation `v`:

1. increment `count`;
2. add `v` to `sum`;
3. find the first configured boundary `b` for which `v <= b`;
4. increment that bucket and every larger cumulative bucket.

An implementation SHOULD use binary search plus either direct cumulative
updates or a final prefix sum. Exact-boundary observations belong to that
boundary's bucket.

### 7.4 Determinism

Emission order MUST be deterministic for tests and human-readable output:

1. configured finite boundaries in ascending order;
2. generated `+Inf` bucket;
3. count;
4. sum.

Logical Histogram groups SHOULD be emitted in deterministic metric-name and
label-tuple order where practical.

## 8. Prometheus exposition

### 8.1 Gauge bucket layout

Version 1 exposes each component with `prometheus.GaugeValue`:

```text
# HELP pg_xact_age_seconds_bucket Open transaction age snapshot in seconds (cumulative bucket)
# TYPE pg_xact_age_seconds_bucket gauge
pg_xact_age_seconds_bucket{datname="app",le="10"} 7
pg_xact_age_seconds_bucket{datname="app",le="30"} 11
pg_xact_age_seconds_bucket{datname="app",le="+Inf"} 12

# HELP pg_xact_age_seconds_count Open transaction age snapshot in seconds (observation count)
# TYPE pg_xact_age_seconds_count gauge
pg_xact_age_seconds_count{datname="app"} 12

# HELP pg_xact_age_seconds_sum Open transaction age snapshot in seconds (observation sum)
# TYPE pg_xact_age_seconds_sum gauge
pg_xact_age_seconds_sum{datname="app"} 187.4
```

The `+Inf` bucket value MUST equal `_count`.

The exporter MUST create separate descriptors for bucket, count, and sum.
The bucket descriptor has the query labels followed by the generated `le`
label. Constant labels apply to all three derived families.

Finite `le` label values MUST be formatted with:

```go
strconv.FormatFloat(boundary, 'g', -1, 64)
```

The generated infinite boundary MUST use the literal `+Inf`. This formatting is
part of the public series identity and MUST be covered by golden tests.

Prometheus quantile interpolation has a separate convention from bucket
storage. When the first finite upper boundary is positive and a quantile falls
in that first bucket, `histogram_quantile()` assumes `0` as the interpolation
lower bound. It does not treat the first bucket's storage interval as
`[0, b0]`, nor does it require an exported `-Inf` bucket. For a signed domain
whose first upper boundary is non-positive, Prometheus returns that upper
boundary when the quantile falls in the lowest bucket rather than extrapolating
toward negative infinity. Consumers MUST account for this lowest-bucket rule;
finite negative boundaries and an explicit zero boundary are therefore
important.

### 8.2 Deliberate metadata limitation

Version 1 does not emit:

```text
# TYPE <base> histogram
```

and does not emit:

```text
# TYPE <base> gaugehistogram
```

The first would misrepresent snapshot buckets as counters. The second requires
an OpenMetrics-aware gatherer and protocol negotiation not provided by the
current `client_golang` collector path.

The Gauge bucket layout preserves correct temporal behavior and remains usable
with classic `histogram_quantile()` because Prometheus evaluates classic
bucket series by the `le` label after ingestion.

Prometheus lint tools may warn that Gauge families use the reserved Histogram
suffixes `_bucket`, `_count`, and `_sum`. Version 1 accepts this warning as the
cost of preserving familiar bucket queries without falsely declaring a
counter-like Histogram. It is not a scrape or PromQL error. Documentation and
CI MUST NOT claim that this layout passes Histogram-specific promlint rules.

### 8.3 Cardinality

For `B` configured finite boundaries and `L` populated label tuples, a logical
Histogram emits:

```text
(B + 3) * L
```

series: `B` finite buckets, one `+Inf` bucket, one count, and one sum.

Reference collectors MUST use low-cardinality grouping labels. They MUST NOT
use `pid`, raw query text, relation name, or other per-object identity labels.

### 8.4 Internal scrape metrics

The query result-size metric SHOULD count the actual number of emitted
Prometheus component metrics. With the Gauge bucket layout, each bucket, count,
and sum is already one `prometheus.Metric`, so no hidden expansion adjustment
is required.

## 9. Cache, reload, and concurrency semantics

Histogram collection reuses the existing query lifecycle:

- A real SQL execution creates a new empty aggregation map.
- Successful completion atomically replaces the cached result with the fully
  buffered scalar and Histogram component metrics.
- A failed execution leaves an empty result for that execution and exposes no
  partial new result. Atomic result publication is a new version 1 guarantee;
  existing scalar row processing does not already provide it.
- A TTL cache hit sends the cached component metrics unchanged.
- A cache hit MUST NOT observe the cached SQL rows again.
- Configuration reload discards prior collector instances and Histogram
  snapshots through the existing re-plan lifecycle.
- Existing collector locks protect Histogram state in the same way as scalar
  metric results.

No background sampler, ticker, persistent state, scrape deduplication, or
cross-scrape accumulation is permitted in version 1.

The existing collector updates its last-execution timestamp after a real
attempt, including a failed attempt. Version 1 retains that behavior: a failed
non-fatal Histogram query caches the empty result until its TTL expires. During
that interval, absence means "no successful snapshot is available", not
necessarily an empty PostgreSQL population. Operators MUST use the existing
query error self-metrics to distinguish collection failure from absence. A
future retry-on-error policy requires a separate lifecycle change.

## 10. Reference PostgreSQL collectors

Version 1.3 is accepted only when it supports the `pg_xact_age` reference query
family and its two logical Histograms.

### 10.1 Open transaction age snapshot Histograms

The compact `pg_xact_age` namespace makes the exported names suitable for
routine dashboard and alert use. Its execution controls are normative:

```yaml
pg_xact_age:
  name: pg_xact_age
  ttl: 10
  min_version: 100000
  tags: [cluster]
```

The `cluster` tag is required because `pg_stat_activity` is cluster-wide. It
prevents auto-discovered database connections from emitting duplicate series.
The minimum version is PostgreSQL 10 because the reference SQL uses
`backend_type`.

PostgreSQL 10+ reference SQL:

```sql
SELECT datname,
       greatest(0, extract(epoch FROM now() - xact_start)) AS seconds,
       CASE WHEN state LIKE 'idle in transaction%' THEN greatest(0, extract(epoch FROM now() - xact_start)) END AS idle_seconds
FROM pg_stat_activity WHERE pid <> pg_backend_pid() AND backend_type = 'client backend' AND datname IS NOT NULL AND xact_start IS NOT NULL;
```

The monitoring role MUST be able to see activity details for other sessions;
membership in `pg_monitor` (or equivalent `pg_read_all_stats` privileges) is
the reference deployment requirement. The WHERE clause filters the nullable
`xact_start` timestamp at the source, so backends without an open transaction
(or with privilege-masked values) are excluded instead of becoming false
zero-valued observations, and the majority idle population is never fetched.

Required label:

```text
datname
```

The reference collector MUST NOT label by `pid`, `usename`, `application_name`,
`query_id`, `wait_event`, `state`, or query text. State semantics are carried by
the two column filters rather than by a label: `seconds` observes every open
transaction that holds an `xact_start`, and `idle_seconds` observes the
idle-in-transaction subset. The `_count` of the `seconds` family therefore
doubles as the current open-transaction count per database.

### 10.2 Open transaction age buckets

The two Histograms observe the same quantity, the age in seconds of an open
transaction measured from `xact_start`, over two nested populations. They use
a `1, 3, 10` logarithmic grid that starts at `1 s`:

```yaml
- seconds:
    usage: HISTOGRAM
    bucket: [1, 3, 10, 30, 100, 300, 1000, 3000, 10000, 30000, 100000]
    description: Open transaction age snapshot in seconds

- idle_seconds:
    usage: HISTOGRAM
    bucket: [1, 3, 10, 30, 100, 300, 1000, 3000]
    description: Idle-in-transaction age snapshot in seconds
```

The grid is a **distribution resolution lattice**, not a list of operating
policies. Within each decade the finite upper bounds are `1` and `3` times a
power of ten, so adjacent ratios alternate between `3` and approximately
`3.33`. The `seconds` family spans `1 s` to `100000 s` (about `27.8 h`), uses
11 finite boundaries, and emits 14 series per populated database including
`+Inf`, count, and sum. The `idle_seconds` family caps at `3000 s` (about
`50 min`), uses 8 finite boundaries, and emits 11 series per populated
database.

Buckets deliberately start at `1 s`. These are **snapshot** Histograms sampled
once per real execution, so they are length-biased by the inspection paradox:
a short-lived object is unlikely to be caught by a 10-second-TTL snapshot, and
sub-second buckets would carry almost no observations. The first bucket
therefore covers `(-Inf, 1]`, and SQL clamps observations to be non-negative.

The two families use different top boundaries on purpose. Open transaction age
extends to `100000 s` so that long batch jobs and bloat-forensics cases remain
visible band by band. Idle-in-transaction age caps at `3000 s` because an idle
transaction older than roughly `50 min` is uniformly critical: its exact age no
longer changes the response, and it is counted in the `+Inf` bucket. The worst
individual idle-in-transaction age remains visible through the existing
`pg_activity_max_tx_duration` Gauge.

The `idle_seconds` population is a strict subset of the `seconds` population.
Every idle-in-transaction backend also holds an open transaction, so for every
database and boundary `idle_seconds_bucket <= seconds_bucket`. The
`idle_seconds` filter uses `state LIKE 'idle in transaction%'`, which includes
the `idle in transaction (aborted)` variant.

These observations are **live ages**, sampled from transactions that still
exist at collection time. They are not completed-transaction latency
distributions. Long-lived transactions are more likely to appear in repeated
snapshots, so statistical interpretation is length-biased. An instant quantile
answers a question about the current live population only.

The right way to read these Histograms is as a **bank of count-over-threshold
gauges**. For each boundary `T`, `_count` minus the cumulative bucket at `T` is
the number of backends whose open (or idle) transaction currently exceeds `T`.
They answer "how many backends are older than `T` right now" for a lattice of
thresholds, rather than describing a latency SLO. Dashboards SHOULD treat `le`
as numeric and use a logarithmic value axis for distribution plots; Section 11
gives the count-over-threshold PromQL form.

The existing `pg_activity` count and maximum-duration Gauges remain unchanged.
The `pg_xact_age` Histogram collector is additive and does not replace existing
series.

## 11. PromQL contract

### 11.1 Instant quantile

Current p95 open transaction age by database:

```promql
histogram_quantile(
  0.95,
  sum by (datname, le) (
    pg_xact_age_seconds_bucket
  )
)
```

### 11.2 Population above a threshold

These Histograms are best read as a bank of count-over-threshold gauges.
Current open transactions older than `3000 s` (about `50 min`), an exact
boundary:

```promql
pg_xact_age_seconds_count
-
ignoring(le) pg_xact_age_seconds_bucket{le="3000"}
```

The same shape over the idle-in-transaction subset answers "how many backends
are stuck idle in transaction beyond a threshold", here `300 s`:

```promql
pg_xact_age_idle_seconds_count
-
ignoring(le) pg_xact_age_idle_seconds_bucket{le="300"}
```

`60 s` is a common policy threshold but not a boundary in this grid. The
Histogram cannot provide an exact `> 60 s` population without interpolation.
Consumers that require that exact threshold SHOULD use a maximum-age or
dedicated threshold Gauge rather than matching a nonexistent `le="60"` series.

Consumers MUST use the boundary string actually exposed by the exporter's
formatting for exact matchers. The current reference boundaries are integers
that render as plain decimals such as `3000` and `100000`; fractional or very
large boundaries in other configurations may render in scientific notation such
as `1e+09`, and consumers MUST match the exact exposed string.

### 11.3 Snapshot mean

Current mean live open transaction age:

```promql
sum by (datname) (pg_xact_age_seconds_sum)
/
sum by (datname) (pg_xact_age_seconds_count)
```

Consumers SHOULD guard division by zero or absent populations.

### 11.4 Prohibited queries

The following forms are semantically invalid for version 1 Histogram series:

```promql
rate(pg_xact_age_seconds_bucket[5m])
increase(pg_xact_age_seconds_count[1h])
irate(pg_xact_age_idle_seconds_bucket[5m])
```

Recording rules and dashboards MUST NOT treat version 1 bucket, count, or sum
families as counters.

## 12. Error handling and observability

Configuration errors fail load with the query branch and column name in the
message.

Collection errors follow the existing query error and fatality behavior. A
Histogram-specific error SHOULD include:

- query name;
- Histogram column name;
- label tuple when safely printable;
- failure category, such as missing column, cast failure, or non-finite value.

The implementation MUST NOT log raw query text or sensitive label values merely
to diagnose Histogram aggregation.

No new self-metrics are required for version 1. Existing query duration,
execution count, error count, cache hit count, and emitted metric count are
sufficient.

## 13. Implementation boundaries

The expected implementation surfaces are:

- `exporter/column.go`: enable `HISTOGRAM`; retain `Bucket []float64`;
- `exporter/config.go`: classify Histogram columns and validate buckets and
  derived names;
- `exporter/query.go`: include logical Histograms in query metric metadata;
- `exporter/collector.go`: aggregate observations and emit component Gauges;
- `exporter/utils.go`: add or use a Histogram conversion path that distinguishes
  NULL-without-default from invalid/non-finite input; the existing
  `castFloat64` returns `NaN` for both and is not sufficient by itself;
- `exporter/validate_labels.go`: reject runtime constant label `le` when any
  parsed query contains a Histogram, using the existing validation call sites
  during exporter creation and reload;
- `config/0000-doc.yml` and `README.md`: document the implemented syntax and
  snapshot PromQL contract;
- `config/0450-pg_xact_age.yml`: provide the two reference open-transaction-age
  snapshot Histograms;
- focused unit and lifecycle tests.

Version 1 SHOULD NOT require changes to the `Server`, `Exporter`, registry,
HTTP handler, or exposition negotiation path.

## 14. Test and acceptance criteria

### 14.1 Configuration tests

Tests MUST cover:

- case-insensitive `HISTOGRAM` usage normalization;
- acceptance of Histogram-only queries;
- empty bucket rejection;
- unsorted and duplicate bucket rejection;
- `NaN`, `+Inf`, and `-Inf` bucket rejection;
- derived metric and `le` label collision rejection;
- rename, scale, and default parsing;
- default multiplied by scale, matching scalar behavior;
- explain/YAML metadata showing configured buckets.

### 14.2 Aggregation tests

Tests MUST cover:

- exact-boundary inclusion with `<=` semantics;
- values below the first and above the last finite bucket;
- generated `+Inf` bucket equal to count;
- correct count and sum;
- multiple label tuples;
- collision-safe label tuple identity;
- multiple Histogram columns in one row set;
- NULL ignored without default;
- NULL observed with default;
- scale applied before bucket assignment;
- invalid and non-finite observations failing the new snapshot;
- one invalid Histogram observation discarding the entire mixed scalar and
  Histogram query result;
- deterministic output order.

### 14.3 Exposition tests

Golden gather tests MUST verify:

- bucket, count, and sum are all `GAUGE` families;
- finite buckets and `+Inf` are present;
- finite `le` values use the specified Go `g` formatting and `+Inf` is literal;
- `le` is appended without disturbing configured label order;
- help strings and constant labels are correct;
- no base `HISTOGRAM` family is emitted;
- emitted metric count includes every component series.

### 14.4 Lifecycle tests

Tests MUST verify:

- a real execution rebuilds the snapshot from zero;
- a cache hit returns identical metrics without accumulation;
- a smaller later population decreases buckets correctly;
- a query failure never exposes a partial replacement;
- a failed execution caches absence until TTL expiry under the existing
  last-execution timestamp policy;
- reload discards old Histogram collectors and cached snapshots;
- concurrent collection remains race-free under the existing lock model.

### 14.5 Reference collector acceptance

The implementation is complete only when:

- the two `pg_xact_age` Histograms gather from a supported PostgreSQL instance;
- the `pg_xact_age` collector runs once with `tags: [cluster]` under
  auto-discovery
  and emits no duplicate series;
- `seconds` and `idle_seconds` each expose their configured boundaries, and the
  idle-in-transaction population is a strict subset of the open-transaction
  population for every database and boundary;
- `seconds` observes every client backend that holds an `xact_start`,
  `idle_seconds` observes only the `idle in transaction%` subset, and each is
  absent for a database with no such backend;
- direct `histogram_quantile()` returns plausible instant values;
- threshold subtraction equals the independently queried SQL count;
- existing Gauge and Counter metric names and values remain unchanged;
- existing tests and race tests pass.

## 15. Non-goals

Version 1 explicitly does not provide:

- separate GaugeHistogram and CounterHistogram configuration types;
- cross-scrape Histogram accumulation;
- completed-query latency distribution;
- exporter-managed event deduplication;
- SQL pre-aggregated bucket ingestion;
- formal OpenMetrics `gaugehistogram` metadata;
- Prometheus Native Histograms;
- OTLP Histogram or ExponentialHistogram export;
- exemplars;
- dynamic or automatically inferred bucket boundaries;
- exporter-computed quantiles;
- high-cardinality per-PID, per-query, or per-relation Histogram labels;
- PostgreSQL 9.x compatibility for the `pg_xact_age` reference query.

## 16. Future evolution

Future work requires a concrete data source and a separate design amendment.

If PostgreSQL or an extension provides reliable pre-aggregated buckets, the
configuration MAY later gain an input layout such as `observations` versus
`buckets`. If monotonically cumulative data is actually available, the same
logical `HISTOGRAM` usage MAY later gain `mode: cumulative`; separate usages are
not required by this specification.

Formal OpenMetrics GaugeHistogram exposition MAY be added behind the existing
configuration through a gatherer or encoder adapter. Native and OTLP encodings
MUST explicitly represent temporality or flavor and MUST NOT infer it from
bucket values.

None of these future possibilities changes the version 1 snapshot contract.

## 17. References

- [Prometheus metric types](https://prometheus.io/docs/concepts/metric_types/)
- [Prometheus histograms and summaries](https://prometheus.io/docs/practices/histograms/)
- [Prometheus `histogram_quantile()`](https://prometheus.io/docs/prometheus/latest/querying/functions/#histogram_quantile)
- [Prometheus Go Collector API](https://pkg.go.dev/github.com/prometheus/client_golang/prometheus#Collector)
- [Prometheus Go default and exponential buckets](https://pkg.go.dev/github.com/prometheus/client_golang/prometheus#ExponentialBuckets)
- [Prometheus Native Histogram specification](https://prometheus.io/docs/specs/native_histograms/)
- [Grafana Heatmap](https://grafana.com/docs/grafana/latest/visualizations/panels-visualizations/visualizations/heatmap/)
- [OpenMetrics GaugeHistogram specification](https://github.com/prometheus/OpenMetrics/blob/main/specification/OpenMetrics.md#gaugehistogram)
- [OpenTelemetry metric data model](https://opentelemetry.io/docs/specs/otel/metrics/data-model/)
- [OpenTelemetry Prometheus compatibility](https://opentelemetry.io/docs/specs/otel/compatibility/prometheus_and_openmetrics/)
- [PostgreSQL `pg_stat_activity`](https://www.postgresql.org/docs/current/monitoring-stats.html#MONITORING-PG-STAT-ACTIVITY-VIEW)
- [PostgreSQL vacuum and freezing parameters](https://www.postgresql.org/docs/current/runtime-config-vacuum.html)
- [PostgreSQL transaction ID wraparound](https://www.postgresql.org/docs/current/routine-vacuuming.html#VACUUM-FOR-WRAPAROUND)
- [Pigsty OLTP PostgreSQL profile](https://github.com/pgsty/pigsty/blob/2d5a45f759274048de0c197829228a71d0182e5c/roles/pgsql/templates/oltp.yml)
- [Pigsty `pg-vacuum` policy](https://github.com/pgsty/pigsty/blob/2d5a45f759274048de0c197829228a71d0182e5c/files/postgres/pg-vacuum)
- [Pigsty PostgreSQL recording and alert rules](https://github.com/pgsty/pigsty/blob/2d5a45f759274048de0c197829228a71d0182e5c/files/victoria/rules/pgsql.yml)

## Appendix A. External review disposition

### A.1 Review identity

| Field | Value |
| --- | --- |
| Reviewer | Claude Code 2.1.201 |
| Model | `claude-fable-5` |
| Effort | `xhigh` (Extra High) |
| Mode | Non-interactive, read-only repository review |
| Initial review session | `02e0e641-d206-4c90-bccb-d1875dbd90fe` |
| Initial result | Conditional approval after required corrections |
| Final verification session | `4462d556-8d70-4c14-b430-00253e3dc429` |
| Final verification result | `FINAL PASS` |

The reviewer read the **version 1.0** specification snapshot and inspected its
referenced exporter, configuration, validation, lifecycle, and test files. It
made no repository edits. Appendix A records that historical review and MUST
NOT be read as a review of the version 1.1 amendment.

### A.2 Confirmed consensus

The review explicitly confirmed these load-bearing decisions:

- one user-facing `HISTOGRAM` usage is sufficient for version 1;
- snapshot-per-real-execution semantics are correct for the then-proposed
  PostgreSQL populations;
- bucket, count, and sum Gauge families are preferable to falsely declaring a
  counter-like `NewConstHistogram`;
- direct `histogram_quantile()` remains valid on cumulative `le` buckets;
- no cross-scrape accumulation, background sampler, or exporter-managed event
  deduplication should be introduced;
- raw observations only, low-cardinality labels, additive reference collectors,
  and the `(B + 3) * L` cardinality model are appropriate for version 1;
- formal GaugeHistogram, Native Histogram, OTLP, and cumulative bucket input
  remain future work.

### A.3 Accepted corrections

The reviewed version 1.0 specification incorporated all correctness findings:

1. It treats query-atomic result publication as a new guarantee rather than an
   existing collector property, and discards the whole mixed query result on
   any Histogram observation failure.
2. It scopes derived-name collision checks to one query definition while
   allowing identical, compatible families across mutually exclusive version
   branches.
3. It assigns explicit execution scope, PostgreSQL version, and TTL controls to
   each reference collector.
4. It specifies stable `le` formatting with `strconv.FormatFloat` and literal
   `+Inf`.
5. It specifies that `default` is scaled and requires a conversion path that
   distinguishes ignored NULL from invalid data.
6. It places runtime constant-label conflict detection in the validation path
   that sees both parsed queries and constant labels.
7. It documents the inherited behavior that a failed real execution caches an
   empty result until TTL expiry.
8. It guards nullable activity timestamps, states the monitoring-role
   requirement, acknowledges promlint suffix warnings, and expands the test
   matrix accordingly.

### A.4 Resolved disagreements and tradeoffs

There are no unresolved blocking disagreements.

The reviewer left whole-query versus Histogram-only discard as an explicit
choice and preferred whole-query discard. The final design adopts whole-query
atomicity because it produces one coherent SQL snapshot and the implementation
can buffer scalar and Histogram metrics in the same local result.

The reviewer suggested placing the constant `le` check in CLI label parsing.
The final design instead assigns it to `validateConstLabelConflicts`, because
that path has both runtime constant labels and parsed query metadata and is
already called during exporter creation and reload. Globally banning `le` in
`parseConstLabels` would reject configurations that contain no Histogram.

The review identified failure-cached absence as an operational tradeoff. The
final design retains existing lifecycle behavior for version 1 rather than
expanding scope into retry policy. It makes the behavior explicit and requires
operators to use existing query error metrics.

The Gauge suffix promlint warning is accepted. Avoiding it would require either
misstating the series as a counter Histogram or abandoning familiar classic
bucket names and PromQL. Neither is preferable for the stated version 1 scope.

### A.5 Final disposition

All required review changes were resolved in version 1.0. That specification
was approved as **Final** for implementation. A second read-only Fable 5 Extra
High review verified each of the eight required corrections against that
repository state and returned `FINAL PASS` with no remaining blocker.

## Appendix B. Version 1.1 post-review amendment

### B.1 Authority and review status

Version 1.1 is a user-directed, post-review amendment. It preserves the
reviewed aggregation, temporality, atomicity, exposition, cache, and validation
contracts. It changes the normative PostgreSQL reference collector and its
bucket policy only.

The implemented amendment received a separate, read-only Claude Code review:

| Item | Value |
|---|---|
| Reviewer | Claude Code |
| Model | `claude-fable-5` |
| Effort | `xhigh` (Extra High) |
| Initial review session | `14632bbe-9078-4ead-9680-ef466c5d4048` |
| Initial result | `FAIL`: one P1 test-coverage gap, no runtime defect |
| Final verification session | `21514615-c953-4340-88f9-ec962fac7df0` |
| Final verification result | `FINAL VERDICT: PASS` |

The reviewer inspected the specification, implementation, tests, reference
configuration, generated configuration, and cited local Pigsty policies. It
also ran the exporter test suite under the race detector. It made no repository
edits.

### B.2 Accepted amendments

Version 1.1 makes the following authoritative changes after operational review:

1. The reference surface is reduced to one cluster-wide `pg_session` query
   with four logical Histograms. Lower-value reference collectors are removed
   from the normative acceptance set.
2. Metric names use the compact `pg_session` namespace and the SQL aliases
   `query_age_seconds`, `xact_age_seconds`, `age_seconds`, and `xid_age`.
3. The three time-age Histograms share a hybrid boundary set: Prometheus's
   familiar short-latency defaults through `10 s`, followed by DBA operational
   anchors through `30 d`.
4. The XID-horizon buckets combine logarithmic early-warning bands with
   PostgreSQL defaults, Pigsty policy, and final wraparound-risk landmarks.
   Those landmarks provide context for backend-held horizons; they do not turn
   backend age into an autovacuum trigger.
5. The classic first-bucket lower edge is explicitly documented as implicit
   negative infinity. Signed domains use finite negative boundaries and zero;
   neither reference configuration nor users configure `-Inf`.

### B.3 Review consensus, correction, and remaining disagreement

The review confirmed the following version 1.1 decisions:

- the PostgreSQL freeze, forced anti-wraparound autovacuum, failsafe, Pigsty,
  and wraparound landmarks are factually correct and version-scoped;
- the document clearly distinguishes relation/tuple freeze triggers from
  backend-held XID horizons, for which the same values are risk landmarks only;
- taking the greater non-negative age of `backend_xid` and `backend_xmin`, and
  omitting the observation only when both are NULL, correctly represents the
  oldest backend-held horizon;
- the hybrid time list is operationally better justified than either pure
  powers of ten or a mechanical `1, 3, 10` ladder;
- classic first-bucket, signed-value, `+Inf`, count, sum, metric naming,
  cardinality, and configuration-generation behavior agree across the spec and
  implementation.

The initial review found one P1 gap: no test pinned a finite `le` value large
enough to exercise scientific notation, although the specification makes that
string part of the public series identity. At the time of the version 1.1
review, `TestHistogramAcceptanceScientificNotationLEIsStable` configured the
then-production `2592000` second boundary and required the literal
`le="2.592e+06"`. The focused test and the full race-enabled suite passed, and
the final verification closed the P1. Version 1.2 retains the same golden-test
contract with its current `3000000` second boundary.

One non-blocking disagreement remains. The reviewer would start the client
session-age buckets near one second because sub-second session ages carry
little operational signal. This specification deliberately keeps all three
second-valued Histograms on the same boundary set. The cost is extra low-value
bucket series for session age; the benefit is one shared dashboard axis, one
recording-rule shape, and direct comparison among query, transaction, and
session populations. The bounded `(B + 3) * L` cost is accepted for version
1.1. This choice may be revisited with production cardinality and query data;
it is not a correctness requirement for future incompatible collectors.

The version 1.2 bucket-policy change is recorded in Appendix C. Future changes
to temporality, input layout, or wire encoding still require an explicit
amendment to this document.

## Appendix C. Version 1.2 logarithmic bucket amendment

### C.1 Authority and scope

Version 1.2 is a user-directed amendment after reviewing how the version 1.1
boundaries behave in distribution plots. It changes only the normative
`pg_session` bucket lists and their policy rationale. It does not change SQL
observations, labels, snapshot temporality, Gauge exposition, aggregation,
atomicity, cache behavior, or validation.

Appendix B remains the historical record of the reviewed version 1.1 design.
This appendix supersedes Appendix B.2 items 3 and 4 and the bucket-policy
conclusion in Appendix B.3. Version 1.2 received a separate read-only formal
review after implementation and live acceptance:

| Item | Value |
|---|---|
| Reviewer | Claude Code |
| Model | `claude-fable-5` |
| Effort | `xhigh` (Extra High) |
| Full review session | `19e03da1-f0ca-4447-b2ef-88b1a9ad9806` |
| Full review result | `FINAL VERDICT: PASS` |
| Final verification session | `5ef9f1f3-452f-4314-a93d-a74251b6cb86` |
| Final verification result | `FINAL VERDICT: PASS` |

Both reviews made no repository edits. The final verification inspected the
strengthened lifecycle/error tests, exact reference-query assertions, committed
pgbench workload, dependency classification, generated configuration, and the
complete build, test, race, vet, tidy, formatting, and static-analysis battery.
It reported no P0 or P1 findings.

### C.2 Resolution lattice versus policy overlay

Version 1.1 mixed two concerns in one boundary list:

- a stable measurement lattice for heatmaps, CDFs, and quantile interpolation;
- exact PostgreSQL, Pigsty, and human operating thresholds.

The resulting time-age ratios ranged from `2` to `7`; XID-age ratios ranged
from `1.05` to `10`. On a logarithmic value axis this creates very wide and
very narrow visual bands. Because a classic Histogram counts observations
rather than normalizing by numeric or logarithmic bucket width, the unequal
bands also make heatmap color intensity difficult to compare. Classic
`histogram_quantile()` uses linear interpolation inside each bucket, so the
same geometry causes uneven estimation resolution.

Version 1.2 makes the Histogram buckets a stable logarithmic resolution
lattice. Operational thresholds remain dashboard and alert policy. A policy
value becomes an additional bucket only after a concrete requirement for an
exact threshold population justifies the cardinality and visual cost.

### C.3 Normative grids and cardinality

The three second-valued Histograms share the 18-boundary `1, 3, 10` grid
`[0.01, 0.03, 0.1, 0.3, 1, 3, 10, 30, 100, 300, 1000, 3000, 10000, 30000,
100000, 300000, 1000000, 3000000]`. Each populated label tuple emits 21
series. The XID Histogram uses the 11-boundary `1, 2, 5` grid `[1000000,
2000000, 5000000, 10000000, 20000000, 50000000, 100000000, 200000000,
500000000, 1000000000, 2000000000]` and emits 14 series per populated tuple.
For one hypothetical label tuple populated in all four Histograms, the
component-series count falls from version 1.1's `90` to `77`. (These are the
version 1.2 grids, recorded inline; the current body documents the narrower
version 1.3 grids.)

The time grid favors a compact, nearly equal Log10 visual spacing across more
than eight decades. The XID grid uses slightly finer spacing because the
`1, 2, 5` sequence naturally retains the most useful PostgreSQL and Pigsty
risk magnitudes. The lowest XID boundary is `1m`; lower ages are intentionally
coalesced into the normal first band.

Dashboard distribution panels SHOULD use a numeric logarithmic value axis.
Existing maximum-age Gauges, database-age metrics, or a future dedicated
threshold Gauge SHOULD carry exact policy alerts. A second threshold-shaped
Histogram is deliberately not introduced.

### C.4 Live acceptance evidence

The version 1.2 reference configuration was exercised on PostgreSQL 18.4 with
an isolated database and a 60-client, 6-thread pgbench workload limited to
approximately one scheduled transaction per second. Each transaction assigned
a real XID, ran one `pg_sleep` selected from `0.3`, `3`, `10`, and `30` seconds,
then held the transaction open with a client-side sleep from the same set. The
60-second run completed 64 transactions with zero failures.

The committed workload is `docs/design/histogram-pgbench.sql`. The exact
invocation was:

```bash
pgbench -h 127.0.0.1 -p 5432 -U postgres \
  -d pg_exporter_hist_v12 -n -c 60 -j 6 -R 1 -T 60 -P 10 -r \
  --random-seed=42 -f docs/design/histogram-pgbench.sql
```

One uncached exporter snapshot contained 55 pgbench sessions grouped as 33
idle, 14 active, and 8 idle in transaction. Its non-cumulative active-query
populations were `1`, `3`, `2`, and `8` in the `(0.3,1]`, `(1,3]`, `(3,10]`,
and `(10,30]` second bands. The idle-in-transaction xact-age populations were
`1`, `2`, and `5` in `(1,3]`, `(3,10]`, and `(10,30]`. All 22 backends holding
an XID were in the first `xid_age <= 1m` band, as expected for a safe short
test.

The live exposition verified all 12 derived families as Gauges, complete
finite boundary sets, monotonically cumulative buckets, `+Inf == count`, and
the required NULL population semantics. Query age appeared only for active
sessions; xact and XID age appeared only for active or idle-in-transaction
sessions; ordinary idle sessions emitted session age without a false zero XID
observation. After pgbench disconnected, every series for the isolated test
database disappeared on the next uncached scrape, confirming replacement
snapshot behavior rather than cross-scrape accumulation.

The test did not attempt to manufacture million- or billion-transaction XID
ages. It validates the XID family structure and first-band behavior, not every
upper risk band.

## Appendix D. Version 1.3 reference-collector redesign

### D.1 Authority and scope

Version 1.3 is a user-directed redesign of the normative reference collector,
dated 2026-07-17. It preserves every engine contract reviewed and accepted in
version 1.0: snapshot-per-real-execution semantics, Gauge bucket/count/sum
exposition, `le` formatting, atomic result publication, NULL handling, cache and
reload behavior, and the `(B + 3) * L` cardinality model. It changes only the
reference query, its labels, its Histogram set, and its bucket policy.

Appendices A, B, and C remain the historical record of versions 1.0, 1.1, and
1.2. Version 1.3 was applied in place: the body of this document (notably
Sections 3, 5, 8, 10, 11, 13, and 14) already describes the version 1.3 design
and remains the sole normative authority. This appendix records what changed
relative to version 1.2 and why. Unlike Appendices A–C it does not record a
separate formal read-only review session.

### D.2 Accepted changes

Version 1.3 makes the following authoritative changes:

1. The reference collector `pg_session` (`config/0415-pg_session.yml`) is
   replaced by `pg_xact_age` (`config/0450-pg_xact_age.yml`). The execution
   controls are unchanged: `ttl: 10`, `min_version: 100000`, `tags: [cluster]`.
2. The four `pg_session` Histograms (`query_age_seconds`, `xact_age_seconds`,
   `age_seconds`, and `xid_age`) are replaced by two Histograms over open
   transaction age: `seconds` over every client backend that holds an
   `xact_start`, and `idle_seconds` over the idle-in-transaction subset. The
   `_count` of `seconds` doubles as the per-database open-transaction count.
3. The `state` label is removed. State semantics move into the two column `CASE`
   filters, cutting cardinality while preserving the idle-in-transaction view.
4. Buckets start at `1 s`. The `seconds` family uses the 11-boundary `1, 3, 10`
   grid `[1 … 100000]` (about `27.8 h`); `idle_seconds` uses the 8-boundary grid
   `[1 … 3000]` (about `50 min`).

### D.3 Rationale

The redesign narrows the reference surface to the one distribution that snapshot
sampling can measure honestly, and reframes what the Histogram is for.

- **Open transaction age is the load-bearing signal.** A long-open transaction
  holds back vacuum horizons, pins bloat, and blocks DDL, and its age is a live
  quantity that a snapshot samples correctly. `idle_seconds` isolates the
  idle-in-transaction subset (the most common pathological case) as a strict
  subset of the same population. `state LIKE 'idle in transaction%'` also
  captures the `(aborted)` variant.
- **Active query age was dropped.** Under autocommit it nearly duplicates
  open-transaction age. Stuck-query detection is a max-type signal already
  served by `pg_activity_max_duration`, and true latency distributions belong to
  `pg_stat_statements`, not to a length-biased snapshot.
- **Session/connection age was dropped.** Snapshot sampling systematically
  misses short-lived connections (length-biased sampling), so it cannot measure
  churn honestly. PostgreSQL 14+ `pg_stat_database` `sessions`/`session_time`
  counters (the `pg_db` collector) measure churn correctly, and
  `pg_activity_max_conn_duration` covers the oldest connection.
- **Backend XID-horizon age was dropped.** Wraparound risk is a max-type signal
  already covered by `pg_xact` and `pg_activity`; a distribution over backend
  XID ages adds no actionable information beyond the current maximum.
- **Sub-second buckets were dropped.** Snapshot Histograms are length-biased by
  the inspection paradox: a 10-second-TTL snapshot almost never catches a
  sub-second object, so those buckets were noise. Buckets start at `1 s`.
- **The two grids differ on purpose.** Open transaction age extends to
  `100000 s` for batch and bloat forensics. Idle-in-transaction age caps at
  `3000 s` because anything older is uniformly critical, its exact value no
  longer changes the response, and it lands in `+Inf`; the worst individual age
  remains visible through `pg_activity_max_tx_duration`.
- **The reframing.** These snapshot Histograms are not latency distributions.
  They answer "how many backends currently exceed age `T`" for a lattice of
  thresholds: a bank of count-over-threshold gauges. Section 11.2 gives the
  canonical count-over-threshold alert form built on this property.
